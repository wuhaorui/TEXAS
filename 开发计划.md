# 德州扑克多人在线 · 开发计划与提示词手册

> 编写日期：2026-09-04
> 代码基线：commit `9a4f1b1`（2026-06-03），此后 3 个月无提交
> 目标：把"能跑通一局"推进到"规则正确、体验完整、可长期维护"

---

## 一、仓库现状盘点

### 1.1 文件清单

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|------|
| `server.js` | 1296 | 全部服务端逻辑（房间、发牌、下注、牌型、边池、断线） | 单文件，无模块拆分，可玩 |
| `public/index.html` | 1756 | 多人版前端（HTML+CSS+Vanilla JS 单文件） | 可玩，体验粗糙 |
| `index.html` | 1496 | **单人练习版**（离线人机） | 2026-04-24 后未维护，且**未被 Express 挂载**（`static` 只指向 `public/`），线上访问不到 |
| `系统架构.md` | 299 | 数据模型 / Socket 事件 / 业务流程 | **部分过时**，见缺陷 P2-1 |
| `package.json` | 14 | express + socket.io + uuid | 缺 test script、缺 engines |
| — | — | README / .gitignore / railway.toml / .env | **全部缺失** |

### 1.2 开发进度（从 git log 提炼的四个阶段）

| 阶段 | 时间 | 主题 | 代表提交 |
|------|------|------|----------|
| ① 骨架期 | 04-24 ~ 04-29 | 单人版 → 多人版重写，建房/加入/下注/摊牌闭环 | `a843c7a` `63284eb` |
| ② 闭环期 | 05-22 | all-in 快进、观战者、rebuy、断线 60 秒重连窗口、非阻塞结算弹窗 | `37b0817` `61bd57f` `88e8689` |
| ③ 体验期 | 05-26 ~ 05-27 | 行动日志、聊天、大屏自适应、先亮牌后判胜、断线跳过 | `b651939` `ae72b0f` `18694b3` |
| ④ 规则期 | 06-01 ~ 06-03 | 30 秒倒计时、筹码封顶防负数、边池含弃牌者贡献、下注额 10 的倍数 | `f4c696f` `08a5c9b` `9a4f1b1` |
| ⏸ 停滞 | 06-03 至今 | 无提交 | — |

**判断**：项目处在"能玩通但没打磨"的状态。核心链路（建房→准备→发牌→下注→摊牌→连局）已经跑通，剩下三块硬骨头：**下注规则正确性**、**状态同步健壮性**、**前端体验与安全**。

### 1.3 功能完成度矩阵

| 功能 | 状态 | 备注 |
|------|------|------|
| 建房 / 加房 / 分享链接 | ✅ 完成 | UUID 前 8 位，URL `?room=XXXX` 直达 |
| 准备 / 房主开始 / 转让房主 / 改名 | ✅ 完成 | |
| 发牌 / 盲注 / 四阶段推进 | ✅ 完成 | SB 10 / BB 20 |
| 下注 / 跟注 / 弃牌 / 全下 | ✅ 完成 | **规则校验不全**，见 P0-1 |
| 过牌（check） | ⚠️ 半完成 | 服务端无 `check` 分支，"什么都不做"侥幸等价于过牌，见 P1-1 |
| all-in 快进 + 边池 | ⚠️ 基本可用 | 有余数丢失、主池赢家判定错位，见 P1-3 / P1-7 |
| 摊牌 / 牌型判定的 9 种牌型 | ⚠️ 有 bug | 同花顺误判（P0-3）、双三条误判（P1-2） |
| 观战者下局自动转正 | ✅ 完成 | |
| all-in 输光自动 rebuy 2000 | ✅ 完成 | 无限 rebuy，无上限无结算 |
| 断线 60 秒重连窗口 | ⚠️ 有 bug | 移除玩家时索引未修正，见 P0-2 |
| 行动日志 / 聊天 | ✅ 完成 | 聊天保留 20 条 |
| 大屏自适应 / 移动端 | ⚠️ 粗调 | 768px 断点只做了牌面缩放 |
| 30 秒倒计时 | ⚠️ 纯前端 | 不与服务端同步，见 P1-5 |
| 下注滑块 / 快捷下注（½pot、pot） | ❌ 缺失 | 只有一个数字输入框 |
| 音效 / 键盘快捷键 / 手牌历史 | ❌ 缺失 | 单人版有快捷键，多人版没有 |
| 边池 UI / 结算筹码动画 | ❌ 缺失 | |

---

## 二、已识别缺陷清单（按优先级）

> 行号基于 commit `9a4f1b1`。

### P0 — 会导致资金错乱或牌局崩溃

| ID | 问题 | 位置 | 说明 |
|----|------|------|------|
| **P0-1** | **下注额可以低于当前注额，导致负数跟注、筹码凭空增加** | `server.js:950-966` | `bet/raise` 只校验 `>0`，且 `room.currentBet = player.currentBet`。preflop BB=20 时某人 bet 10 → `currentBet` 被拉低到 10 → 大盲玩家点跟注时 `toCall = 10 - 20 = -10` → `player.chips -= -10`（筹码变多）、`room.pot += -10`（底池变少）。前端只有静态 `min="10"`，挡不住。 |
| ~~**P0-2**~~ | ~~**断线玩家被移除时未修正索引**~~ **（2026-09-04 已修复）** | `server.js` `removePlayerFromRoom()` | ~~`splice(pi, 1)` 后，`room.currentPlayer`、`room.dealer`、以及 `room.actedThisPhase`（**存的是数组下标**）全部错位，会指到错误的玩家身上，直接毁掉这一局。~~<br>修复：新增唯一 splice 出口 `removePlayerFromRoom(room, playerIndex)`，内部按「移除前算继承者 → splice → 重映射 → 归一化 → 房主转让 → 人数不足回 waiting → 广播」处理；配套 `nextActionableIndex()` / `remapStateAfterPlayerRemoval()` / `normalizeRoomIndices()`。测试：`test-remove-player.js`（49 项）、`test-e2e-remove-player.js`（26 项）。 |
| **P0-3** | **同花顺误判** | `server.js:83-100` | 进入条件是 `isFlush && (isStraight \|\| isWheel)`，但 `isStraight` 是**全 7 张牌**的判定，可能顺子根本不在同花那一门里。此时 `sfHigh` 为 0，代码仍把 `handName` 定为"同花顺"。例：♠A♠K♠Q♠J♠9 + 其他两门凑出 5-6-7-8-9 顺子 → 应判"同花"，实际判"同花顺"。 |

### P1 — 规则/体验明显错误

| ID | 问题 | 位置 | 说明 |
|----|------|------|------|
| **P1-1** | `check` 无服务端分支、无合法性校验 | `server.js:927-977` | 有注额时误发 check = 免费看牌，破坏牌局。应校验 `player.currentBet === room.currentBet`，否则拒绝。 |
| **P1-2** | 双三条（3+3+1）被判为"三条" | `server.js:124-130` | 7 张牌中两组三条时应判"葫芦"。当前 `c1===3 && c2===2` 不匹配。`bestValues` 数值恰好等价，所以**只有牌型名显示错**，但结尾显示错误很扎眼。 |
| **P1-3** | 边池分配用 `Math.floor` 逐层取整，余数凭空消失 | `server.js:205` | 每层最多丢 `tied.length-1` 筹码，一局可能丢几个。应累计余数、分给主池赢家。 |
| **P1-4** | 前端全量 `innerHTML` 重渲染 + XSS | `public/index.html:1563-1591`、`1601-1616`、`1181/1404/1555` | 每次 `gameUpdate` 重建全部座位 → 已翻开的公共牌反复播放发牌动画；昵称/聊天文本直接拼进 `innerHTML`，恶意昵称可注入脚本。 |
| **P1-5** | 倒计时纯前端、与服务端不同步 | `public/index.html:1624-1665` | 各端独立计时，超时本地直接发弃牌；网络延迟或重连后会误弃牌。应由服务端下发 `actionDeadline` 时间戳。 |
| **P1-6** | "判胜 + 延迟开新局"逻辑重复 5 处 | `server.js:284-288 / 433-450 / 494-502 / 1014-1030 / 1249-1256` | 每处约 30 行几乎相同。任何一处漏改都会出新 bug（历史上已经漏过两次：`964dd69`、`6190d39` 都是在补漏）。必须抽成 `endHandEarly()` + `scheduleNextHand()`。 |
| **P1-7** | 主池赢家名单与边池实际分钱名单不一致 | `server.js:222-235` | `winners` 返回的是"并列第一"的人，而真正加钱的是边池循环里"任何层级赢过钱"的人。短码玩家赢了边池却不被标为 winner，或反之。 |

### P2 — 工程化与文档

| ID | 问题 | 位置 | 说明 |
|----|------|------|------|
| **P2-1** | 架构文档过时 | `系统架构.md:114/117/118` | 文档写的 `phaseAdvanced`、`gameOver`、`newHand` 三个事件**实际不存在**；实际有的 `rejoinRoom`、`renamePlayer`、`chatMessage`、`requestGameState`、`playerRenamed` **文档没写**。 |
| **P2-2** | 房间无清理、无持久化、无健康检查 | `server.js:24` | `rooms` 只增不减（仅玩家全走时删除），无空闲房间 GC；进程重启房间全丢；Railway 无 `/health` 探针。 |
| **P2-3** | 零测试 | — | `evaluateHand` / `getWinners` 是纯函数，最适合单测，但目前没有任何测试。上面 P0-3、P1-2 两个 bug 本可以被测试拦住。 |
| **P2-4** | 无最小加注额规则 | `server.js:950-966` | 标准规则：短码 all-in 的加注额若不足最小加注增量，其他玩家**只能跟注或弃牌，不能再加注**。当前无此限制。 |
| **P2-5** | 单人版 `index.html` 成孤儿 | 根目录 | 1496 行单机版未被 Express 挂载，线上访问不到。要么挂载到 `/solo`，要么归档到 `legacy/`。 |
| **P2-6** | 缺项目配置文件 | — | 无 README、无 `.gitignore`、无 `railway.toml`（架构文档 §8.1 明确要求 `replicas = 1`，但仓库里没有这个文件）。 |
| **P2-7** | 死代码与调试残留 | `server.js:347/1223/335` | `room.phaseBets` 定义从未使用；`disconnect` 里 `const betActions` 未使用；`createRoom(hostId)` 的 `hostId` 参数未使用。前端大量 `console.log` 残留。 |

---

## 三、开发路线（5 个阶段）

```
阶段 1  止血（规则正确性）      → P0-1 / P0-2 / P0-3 / P1-1 / P1-2 / P1-3
阶段 2  重构（消除重复）        → P1-6 / P1-7，把牌局流程收拢成状态机
阶段 3  前端体验                → P1-4 / P1-5，下注滑块、快捷键、音效、边池 UI
阶段 4  工程化                  → P2-2 / P2-3 / P2-6，测试 + 配置 + 部署
阶段 5  产品化（可选）          → 计分板、手牌历史、旁观、移动端重做
```

| 阶段 | 目标 | 建议顺序 | 预估工作量 |
|------|------|----------|-----------|
| 1 | 堵住所有"钱算错"的路径 | 提示词 1 → 2 → 3 → 4 | 半个工作日 |
| 2 | 让牌局流程只有一份代码 | 提示词 5 → 6 | 半个工作日 |
| 3 | 前端从"能用"到"好用" | 提示词 7 → 8 → 9 → 10 | 1 个工作日 |
| 4 | 可测试、可部署、可交接 | 提示词 11 → 12 → 13 | 半个工作日 |
| 5 | 产品化增强 | 提示词 14 → 15 | 按需 |

> 强烈建议：**阶段 1 完成后先 commit 一次**，再动阶段 2。历史上多次出现"改 A 处漏了 B 处"的回归（`964dd69`、`6190d39`），就是因为同样的逻辑散落在 5 个地方。

---

## 四、可直接使用的提示词

### 使用说明

- 提示词按执行顺序编号，每条**自包含**（含项目路径、技术栈、约束），可直接整段复制。
- 每条都带**验收标准**，改完必须逐条自查通过再提交。
- 通用约束（所有提示词默认生效，无需重复说明）：
  - 项目根目录 `/Users/wuhaorui/WorkBuddy/本地虾/德`
  - 服务端是**单文件** `server.js`，当前阶段**不要**拆分成多模块（拆分放到阶段 2 之后单独做）
  - 保持现有 Socket 事件命名与数据字段，除非提示词明确要求变更
  - 每次改完执行 `node --check server.js` 确认语法通过
  - 前端是 `public/index.html` 单文件，禁止引入构建工具和前端框架

---

### 提示词 1：修复下注金额校验（P0-1，最高优先级）

```text
在 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js 中修复下注金额的合法性校验问题。

【背景】
这是 Node.js + Socket.io 的德州扑克服务端，全部逻辑在单个 server.js 里。
当前 `bet` 事件处理（约 950-966 行）对 bet/raise 只校验了 amount > 0，存在严重资金 bug：
- 玩家 A（非盲注位，currentBet=0）在 preflop 下注 10，代码执行 `room.currentBet = player.currentBet` 后 currentBet 被拉低到 10；
- 此时轮到大盲玩家（currentBet=20）点跟注，`toCall = room.currentBet - player.currentBet = 10 - 20 = -10`；
- 负数会导致 `player.chips -= -10`（筹码凭空增加）且 `room.pot += -10`（底池变少）。

【要求】
1. 在 room 对象上新增下注规则状态：
   - `minRaise`：当前最小加注增量，startNewHand 时初始化为 `room.bigBlind`；每次合法加注后更新为 `(本次加注后的 currentBet) - (加注前的 currentBet)`。
   - `lastRaiseIndex`（可选）：最后一次加注者的索引，用于判断一轮是否结束。
2. 新增一个纯函数 `validateBetAction(room, player, action, amount)`，返回 `{ valid: boolean, error?: string, normalizedAmount?: number }`，统一处理以下规则：
   - `fold`：永远合法。
   - `check`：仅当 `player.currentBet === room.currentBet` 时合法，否则返回错误"当前有下注，不能过牌"。
   - `call`：计算 `toCall = room.currentBet - player.currentBet`；若 `toCall <= 0`，**归一化为 check**（不移动任何筹码），不要出现负数；若 `toCall > player.chips`，归一化为 all-in（只投入剩余筹码）。
   - `bet` / `raise`：
     - 要求 `player.currentBet + amount >= room.currentBet + room.minRaise`，否则拒绝并给出明确错误提示（如"最小加注到 X"）。
     - 特例：若玩家全部筹码投入后仍达不到最小加注额，允许该次动作按 all-in 处理（短码 all-in）。
     - 禁止把 `room.currentBet` 调低；`room.currentBet = Math.max(room.currentBet, player.currentBet)`。
   - `allin`：永远合法，投入 `player.chips`。
3. 在 `bet` 事件处理开头调用该函数，`valid === false` 时直接 `callback({ success: false, error })` 并 return，不改动任何状态。
4. 保证 `room.pot`、`player.chips`、`player.currentBet`、`room.currentBet` 四者的加减永远非负，可在关键位置加 `Math.max(0, ...)` 兜底。
5. 同步更新前端 public/index.html 的下注校验（约 1143-1163 行 playerAction 及 826-828 行的 bet-input）：
   - 输入框的 min 值随 `room.currentBet + minRaise` 动态变化，max 值为自己的剩余筹码；
   - 非法数值时按钮置灰并提示原因。

【验收标准】
- preflop BB=20 时，任何玩家都无法把当前注额压到 20 以下；尝试下注 10 会收到明确错误且状态不变。
- 构造场景：A 下注 10（被拒）→ 改为下注 40 → B 跟注 → 大盲位过牌。全程所有玩家 chips 与 pot 之和恒等于房间初始总筹码。
- `node --check server.js` 通过；本地起服务，两人对局跑通 3 局无负数、无 NaN。
- 服务端增加一行日志输出每次动作的 `{action, amount, beforeChips, afterChips, pot}`，便于核对。
```

---

### 提示词 2：修复断线玩家移除时的索引错位（P0-2）

```text
在 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js 中修复玩家移除导致的索引错位 bug。

【背景】
`disconnect` 事件里的 60 秒超时定时器（约 1271-1288 行）会执行 `currentRoom.players.splice(pi, 1)` 移除断线玩家。
但 room 里有多处状态存的是**数组下标**而不是玩家 id：
- `room.currentPlayer`（当前行动者下标）
- `room.dealer`（庄家下标）
- `room.actedThisPhase`（Set，存的是本阶段已行动玩家的下标）

splice 之后数组长度变短，这些下标会指向错误的玩家，直接毁掉当前这一局（可能表现为"还没轮到你"、"发牌错位"、"庄家位置乱跳"）。

【要求】
1. 新增工具函数 `removePlayerFromRoom(room, playerIndex)`，集中处理移除逻辑，禁止在其他地方直接 splice。
2. 在移除前，先把三个下标类状态转换成"待移除玩家之后的相对偏移"，移除后再重算：
   - 若 `room.currentPlayer > playerIndex`，移除后 `room.currentPlayer -= 1`；若等于 playerIndex，需要先把行动权移交给下一个合法玩家（复用现有的"寻找下一个可行动玩家"逻辑），再减 1。
   - 若 `room.dealer > playerIndex`，移除后 `room.dealer -= 1`；若等于 playerIndex，庄家顺延到前一个位置（保持 dealer 相对顺序不变）。
   - `room.actedThisPhase` 必须重建：把 Set 里所有下标映射到移除后的新下标，并丢弃等于 playerIndex 的项。
3. 移除后做一致性检查：
   - `room.currentPlayer` 和 `room.dealer` 必须在 `[0, room.players.length - 1]` 范围内，越界则对 length 取模。
   - 若 `room.players.length < 2`，把 `room.phase` 置为 `waiting`、`room.gameStarted = false`，并广播提示"人数不足，等待新玩家"。
4. 移除后向房间广播完整状态（`playerJoined` + `gameUpdate` 或 `gameStarted`，视 `gameStarted` 而定），避免前端停留在旧座位布局。

【中期建议（本次可先不做，但请在代码注释里标注 TODO）】
把 `room.currentPlayer` / `room.dealer` / `room.actedThisPhase` 从"下标"改为"玩家 id"存储，从根本上消除这类问题。

【验收标准】
- 3 人开局，让处于中间位置的玩家断线并等待 60 秒被移除，游戏继续：庄家位置、行动顺序、已行动标记全部正确，剩余两人能正常打完这一局。
- 让当前行动者本人断线并被移除，行动权正确移交给下一位，不出现"还没轮到你"死锁。
- 剩余 1 人时游戏回到 waiting 状态，新玩家加入后可重新开始。
```

---

### 提示词 3：修复牌型判定 bug（P0-3 / P1-2）并补测试

```text
在 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js 中修复 evaluateHand 的两个牌型判定错误。

【背景】
evaluateHand（39-152 行）从 7 张牌里挑最好的 5 张。目前发现两处错误：

错误 1（严重，P0-3）——同花顺误判：
进入同花顺分支的条件是 `isFlush && (isStraight || isWheel)`，但 isStraight / isWheel 是对**全部 7 张牌**算的，顺子可能根本不在同花那一门里。
分支内部虽然重新用同花牌算了 sfHigh，但算出来为 0（即同花牌不构成顺子）时，代码依然把 handName 定为"同花顺"。
示例：手牌 ♠A ♠K，公共牌 ♠Q ♠J ♠9 ♥8 ♦7 —— ♠ 是同花但不构成顺子，7 张牌里也没有顺子，正确结果应为"同花 A 高"，当前被判为"同花顺"。

错误 2（显示错误，P1-2）——双三条被判成三条：
7 张牌里出现两组三条（3+3+1）时，counts 排序后是 c1=3、c2=3，匹配不上 `c1 === 3 && c2 === 2` 的葫芦分支，直接落到"三条"分支。
此时应当用较大的三条 + 较小的三条里任意两张组成葫芦。

【要求】
1. 重构同花顺判定：先判断是否存在同花（≥5 张同花色），再**只在该花色的牌里**判断顺子（含 A-2-3-4-5 的轮子）。只有同花牌确实构成顺子时才判"同花顺"/"皇家同花顺"，否则按同花/顺子的优先级正常降级。
2. 修正葫芦判定：把"有至少一组三条，且除该三条外还能凑出至少一对（含用第二组三条充当对子）"的情况统一判为葫芦，bestValues 取 `[三条值×3, 对子值×2]`，对子值取剩余牌中能成对的最大值。
3. 保持 evaluateHand 的返回结构不变：`{ handRank, handName, tieBreaker, values }`，values 固定 5 个元素（降序），避免影响 getWinners 的比较逻辑。
4. 新增测试文件 test/hand-eval.test.js（使用 Node 内置 node:test 模块，不要引入新依赖），至少覆盖以下用例：
   - 皇家同花顺、普通同花顺、A-2-3-4-5 同花顺、四条、葫芦（含双三条构造的葫芦）、同花、顺子、三条、两对、一对、高牌 各 1 组；
   - 上面"错误 1"的示例必须判为同花；
   - 双三条 3+3+1 必须判为葫芦；
   - 同牌型比大小：两对比最大对 → 比第二对 → 比踢脚；同花比最高牌序列。
5. 在 package.json 里加 `"scripts": { "start": "node server.js", "test": "node --test test/" }`。

【验收标准】
- `npm test` 全部通过。
- 用暴力枚举交叉验证（可选但建议）：随机生成 20000 组 7 张牌，确认每种牌型都出现在合理频次区间内，且不出现"同花顺比皇家同花顺更多"这类反常分布。
- 修复后重启服务，本地跑几局确认摊牌牌型显示正常。
```

---

### 提示词 4：修复边池余数丢失（P1-3）

```text
在 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js 的 getWinners 函数（154-236 行）中修复边池分配的取整问题。

【背景】
边池按贡献层级切分，每层执行 `share = Math.floor(increment * allInLevel / tied.length)`（约 205 行）。
每层向下取整的余数被直接丢弃，一局多层下来会凭空消失几个筹码，导致"房间总筹码不等于初始总筹码之和"。

【要求】
1. 在层级循环里用变量 `carry` 累计每层取整丢掉的余数（`remainder = increment * allInLevel - share * tied.length`），在下一层分配时补进去；循环结束后若仍有余数，按"主池赢家优先，其次按座位顺序"分配给赢家（每人最多 1 筹码）。
2. 分配结束后加一条断言（或 console.warn）：所有赢家分到的钱 + 未分配残值 === 进入 getWinners 时的 room.pot。若不等，打印完整调试信息（各玩家 totalPotBet、层级列表、每层分配明细）。
3. 保留现有的"弃牌玩家贡献也计入边池"逻辑，不要回退这个行为。

【验收标准】
- 3 人局，筹码分别为 100 / 500 / 1000，全部 all-in，摊牌后三人赢到的筹码之和 === 进入摊牌时的 pot。
- 平局分裂底池（两人完全相同牌型）时，若 pot 为奇数，多出的 1 筹码归属明确且不丢失。
- 服务端日志打印每层边池的 {level, eligible, allInLevel, increment, share, remainder}，便于人工核对。
```

---

### 提示词 5：抽取牌局结束与开新局的公共逻辑（P1-6 / P1-7）

```text
重构 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js，把重复 5 次的"提前结束牌局 + 延迟开新局"逻辑收敛成两个函数。

【背景】
以下 5 处几乎完全相同的代码块（每处约 30 行）：
- skipDisconnectedCurrent 内：约 277-288 行
- advancePhase 的"只剩一人"分支：约 417-449 行
- advancePhase 的"全员 all-in 快进"分支：约 479-502 行
- bet 事件里"其他人全弃牌"分支：约 997-1030 行
- disconnect 事件里"当前玩家断线"分支：约 1242-1256 行

每处都做了：发 gameEnd → setTimeout → handleRebuy → 过滤可继续的玩家 → startNewHand → 发 gameStarted。
历史上已经因为"改了一处漏了另一处"出过两次线上 bug（commit 964dd69、6190d39）。

【要求】
1. 新增 `endHand(room, { winners, handName, results, delayToNext = 8000 })`：
   - 统一结算 pot（把 room.pot 清零并记入 wonPot）、统一广播 `gameEnd`；
   - 用 `room._nextHandTimer` 保存定时器句柄，**开新局前先 clearTimeout 上一个未触发的定时器**，杜绝重入时出现两条并行的新局；
   - 延迟结束后调用 `scheduleNextHand(room)`。
2. 新增 `scheduleNextHand(room)`：
   - `handleRebuy(room)` → 过滤 `chips >= bigBlind && !isSpectator && !disconnected` 的玩家；
   - 人数 ≥ 2 时 `startNewHand(room)` 并广播 `gameStarted`（字段与现有保持一致）；
   - 人数 < 2 时把房间置回 `waiting`、`gameStarted = false`，并广播 `playerJoined` 让前端回到等待界面。
3. 把上述 5 处全部替换为对新函数的调用，删除重复代码。不同场景只是参数不同：
   - 全员弃牌 / 断线弃牌：winners = 唯一未弃牌者，handName = '对手全弃牌'，延迟 3000ms；
   - 全员 all-in 快进：先发一次 gameUpdate 亮出公共牌，延迟 5000ms 后 endHand，再延迟开新局；
   - 正常摊牌：延迟 5000ms 发结果，8000ms 后新局。
4. 修复 P1-7：`gameEnd` 的 `winners` 字段必须与实际拿到筹码的玩家名单一致。把边池循环里"任何层级赢过钱"的玩家收集起来作为 winners（按赢到的金额降序），handName 取主池赢家的牌型名。

【约束】
- 不要改变任何 Socket 事件的名称和现有字段（前端依赖它们）。
- 不要借机重写下注流程（下注流程重构放到后续单独做）。
- 每替换一处就跑一遍对应场景，确认行为与重构前一致。

【验收标准】
- 5 处调用点全部替换完成，全文搜索 `setTimeout` 在牌局流程里只剩 endHand / scheduleNextHand 内部各 1 处。
- 逐一验证 4 个场景仍然正常：①有人弃牌只剩 1 人 ②全员 all-in ③正常河牌摊牌 ④当前玩家断线。每个场景都能正确结算并自动开下一局。
- 快速连续触发两次结束条件（例如两人同时断线），不会出现重复的 gameStarted 或负数筹码。
```

---

### 提示词 6：补充架构文档与接口对齐（P2-1）

```text
更新 /Users/wuhaorui/WorkBuddy/本地虾/德/系统架构.md，让它与代码完全一致。

【背景】
文档写于项目早期，现在已经过时。实际代码与文档的差异（以 server.js 和 public/index.html 当前版本为准）：
- 文档 §4.2 列出的 `phaseAdvanced`、`gameOver`、`newHand` 三个事件**代码中根本不存在**（实际由 gameUpdate / gameStarted 承担）；
- 代码中实际存在但**文档没记录**的事件：`rejoinRoom`、`renamePlayer`（对应广播 `playerRenamed`）、`chatMessage`、`requestGameState`；
- 文档 §3.1 的 Room 数据模型缺少实际使用的字段：`actedThisPhase`（Set）、`previousBet`、`minRaise`（提示词 1 新增）、`gameStarted` 以外的观战/断线相关状态；
- 文档 §3.2 的 Player 模型缺少：`isSpectator`、`rebuyCount`、`disconnected`、`totalPotBet`、`socketId`；
- 文档没有描述：观战者转正规则、all-in 输光自动 rebuy 2000 的规则、断线 60 秒重连窗口、30 秒下注倒计时、边池计算方式。

【要求】
1. 逐条核对代码，把 §3 数据模型、§4 Socket 事件表更新为实际情况（不存在的事件直接删除，不要保留"计划中"的条目）。
2. 新增章节描述：观战者机制、rebuy 机制、断线重连机制、边池分配算法（含贡献层级的定义和余数处理）、下注合法性规则（最小加注额、短码 all-in 特例）。
3. 在文档开头加一个"文档版本与代码基线"小节，注明本文档对应的 commit hash 和更新日期，方便以后判断文档是否再次过期。
4. 不要删除 §6 牌型大小、§8 部署配置、§9 界面结构这些仍然有效的内容。

【验收标准】
- 文档里出现的每一个 Socket 事件名，都能在 server.js 中用 grep 找到对应的 emit 或 on。
- 反过来，server.js 里每一个 io.to(...).emit 和 socket.on，文档里都有记录。
- 让一个没看过代码的人照着文档能完整理解一局牌从创建房间到摊牌的全过程。
```

---

### 提示词 7：前端改为增量更新 + 修复 XSS（P1-4）

```text
重构 /Users/wuhaorui/WorkBuddy/本地虾/德/public/index.html 的渲染逻辑。

【背景】
updateGameState（约 1451-1622 行）每次收到 gameUpdate 都用 innerHTML 全量重建所有座位和公共牌，带来两个问题：
1. 动画重复播放：已经翻开的公共牌会在每次 gameUpdate 时重新触发 dealing 发牌动画；座位上已展示的手牌也会闪烁重绘。
2. XSS：玩家昵称（约 1181、1555 行）、聊天文本（约 1404 行）、结算弹窗里的文本（约 1436 行）都通过字符串拼接直接塞进 innerHTML，恶意昵称可以注入脚本。

【要求】
1. 新增 `escapeHtml(str)` 工具函数，所有插入 innerHTML 的用户可控内容（昵称、聊天文本、牌型名、房间号）必须经它转义。禁止用 innerHTML 拼接任何用户输入；聊天消息优先改用 textContent 赋值。
2. 公共牌改为增量渲染：维护一个 `renderedCommunityCount`，只有新增的牌才播放发牌动画，已渲染的牌只更新内容不重放动画。同理，座位卡片改为按玩家 id 做 key 的 diff 更新——已存在的座位只更新筹码/下注额/状态徽章，不重建 DOM 节点。
3. 玩家手牌只在 `gameStarted` 和自己 id 匹配时渲染一次，后续 gameUpdate 不要重绘（除非手牌内容确实变了）。
4. 保留现有的发牌动画视觉效果（dealing 动画、翻牌动画），不要因为重构丢失。
5. 顺手清理生产环境不需要的 console.log，保留 console.error / console.warn。

【约束】
- 不引入任何前端框架或构建工具，保持纯 Vanilla JS 单文件。
- 不要改动 socket 事件的处理逻辑，只改渲染层。

【验收标准】
- 一局牌中连续触发 10 次 gameUpdate（连续跟注），公共牌动画只在真正发新牌时播放，已翻开的牌不闪烁。
- 昵称输入 `<img src=x onerror=alert(1)>` 的玩家加入后，页面不弹窗、不执行脚本，昵称原样显示为文本。
- 聊天发送 `<script>alert(1)</script>` 同样安全。
- 本地 2 人对局跑完整流程，视觉表现与重构前一致（动画、布局、徽章位置不变）。
```

---

### 提示词 8：下注交互重做（滑块 + 快捷下注 + 校验）

```text
重做 /Users/wuhaorui/WorkBuddy/本地虾/德/public/index.html 的下注操作区。

【背景】
当前下注区（约 826-828 行 bet-input、1143-1163 行 playerAction）只有一个数字输入框，min 硬编码为 10，缺少成熟德州扑克产品必备的下注交互：
- 没有下注滑块；
- 没有 ½ 底池 / 底池 / 1.5 倍底池 / 2 倍底池 这类快捷按钮；
- 没有最小加注额提示，用户不知道该下多少；
- 单人版 index.html 里已有的键盘快捷键（F 弃牌 / C 过牌跟注 / B 下注 / R 加注 / A 全下）在多人版里没有实现。

【要求】
1. 把操作区改成两行：
   - 第一行是快捷按钮：`½ Pot`、`Pot`、`1½ Pot`、`2× Pot`、`All In`；
   - 第二行是滑块 + 数字输入框 + `最小 / 最大` 快捷跳转，滑块下方实时显示"下注到 X（本次投入 Y）"。
2. 滑块/输入框的取值范围由服务端下发的状态推导，不要硬编码：
   - `min` = `currentBet + minRaise`（加注场景）或 `bigBlind`（无人下注时的首次下注场景）；
   - `max` = 自己的剩余筹码；
   - 若 `min > max`（短码情况），把滑块禁用并把按钮切换为"All In"。
3. 按钮可用性跟随状态：
   - 有注额未匹配时隐藏"过牌"、显示"跟注 X"；已匹配时显示"过牌"、隐藏"跟注"；
   - 剩余筹码不足最小加注额时，"加注"按钮置灰并提示"筹码不足最小加注额"。
4. 加键盘快捷键：F 弃牌、C 过牌/跟注、B 下注（聚焦输入框）、R 加注、A 全下。输入框获得焦点时禁用快捷键（避免打字触发）。
5. 金额输入做千分位友好的即时校验：非数字、非 10 的倍数值给出即时提示（保留现有"10 的倍数"规则）。
6. 服务端需要配合下发 `minRaise` 字段：在 gameUpdate / gameStarted 的 payload 里加上 `minRaise: room.minRaise`（若提示词 1 已完成则已有该字段）。

【验收标准】
- 拖动滑块时，上方实时显示"下注到 X（本次投入 Y）"，X 和 Y 的差值 = 本次从筹码里拿走的钱，与实际扣款一致。
- 快捷按钮点 `Pot` 后，投入额 = 当前 pot + 需要跟注的部分，与服务端实际扣款一致。
- 短码玩家（筹码 < 最小加注额）只能跟注或全下，加注按钮置灰。
- 键盘 F/C/B/R/A 在轮到自己时全部生效，在输入框聚焦打字时不误触发。
- 在网络延迟下快速连点两次下注按钮，不会发送两次下注（操作后立刻禁用按钮直到收到新的 gameUpdate 或超时 2 秒）。
```

---

### 提示词 9：服务端驱动的倒计时（P1-5）

```text
把 /Users/wuhaorui/WorkBuddy/本地虾/德 的下注倒计时改为服务端驱动。

【背景】
当前倒计时（public/index.html 约 1624-1665 行）是纯前端的 30 秒固定计时，存在三个问题：
1. 各端独立计时，不同玩家的剩余时间不一致；
2. 超时后前端自己发 `fold`，若此时网络有延迟或刚重连，可能在轮到别人时误发弃牌；
3. 玩家重连后计时器从头开始，等于无限续时。

【要求】
1. 服务端：
   - 在轮到某玩家行动时（bet 事件处理末尾、advancePhase、startNewHand 之后），记录 `room.actionDeadline = Date.now() + ACTION_TIMEOUT`（常量 30000，定义在文件顶部配置区）；
   - 在 `gameUpdate` 和 `gameStarted` 的 payload 里下发 `actionDeadline` 和 `serverTime`（`Date.now()`）；
   - 新增服务端超时检查：用一个 `setInterval`（每 2 秒）扫描所有 gameStarted 的房间，若 `room.actionDeadline` 已过且该玩家未行动，服务端主动按 `fold` 处理（复用提示词 1 之后的 validateBetAction 通道），并广播 `playerAction`。每次轮转时清除旧的 deadline。
2. 前端：
   - 收到 `actionDeadline` 后用 `actionDeadline - (Date.now() - (serverTime 与本地时间的偏移))` 计算剩余毫秒，用 requestAnimationFrame 或 setInterval(100ms) 刷新进度条；
   - 倒计时归零后**不要**自己发 fold，等待服务端广播；前端只做"即将超时"的视觉预警（最后 5 秒变红 + 心跳动画）；
   - 断线重连时通过 `requestGameState` 拿到最新的 actionDeadline，计时器自动对齐。
3. 顺手加上视觉细节：当前行动者的座位下方显示一条倒计时进度环/进度条。

【验收标准】
- 两个浏览器同时打开同一个房间，两边显示的剩余秒数差不超过 1 秒。
- 玩家 A 超时后，服务端统一判弃牌，所有端（包括 A 自己）都看到"XXX 超时弃牌"，且行动权正确移交给下一位。
- 玩家刷新页面重连后，倒计时接续剩余时间而不是重新计时 30 秒。
- 服务端超时判负与玩家主动点弃牌走完全相同的代码路径，行为完全一致。
```

---

### 提示词 10：边池可视化与结算动画

```text
为 /Users/wuhaorui/WorkBuddy/本地虾/德 增加边池展示和结算动画。

【背景】
服务端已经实现了边池分配（getWinners 里的贡献层级算法），但前端完全看不到：多人 all-in 时，玩家不知道有几个池、每个池多少钱、谁有资格赢哪个池。同时摊牌时筹码是瞬间到账的，缺少"筹码推给赢家"的结算反馈。

【要求】
1. 服务端：在 `gameEnd` 的 payload 里增加 `pots` 字段，结构为
   `[{ amount: number, eligiblePlayerIds: string[] }]`，由 getWinners 在层级循环里顺带收集（不改变现有分配结果，只把过程数据吐出来）。
2. 前端：
   - 当 `pots.length > 1` 时，在底池区把单一 pot 数字替换为多个池的堆叠显示：`主池 ¥X` + `边池1 ¥Y（资格：A、B）`，并有明显的视觉分隔；
   - 每个池用不同颜色的小筹码堆表示。
3. 结算动画：
   - `gameEnd` 触发后，从底池位置生成筹码飞向赢家座位的动画（CSS transform + transition 即可，约 800ms），赢家座位同时高亮 + 显示 `+¥N`；
   - 平局分裂时筹码分别飞向每个赢家；
   - 动画结束后再弹出结算模态框（现有 game-end-modal），避免遮住动画。
4. 动画时长计入现有的 5 秒亮牌窗口内，不要因此延长整局节奏；如果用户设备 prefers-reduced-motion，跳过动画直接显示结果。

【验收标准】
- 3 人以上、出现 all-in 导致多层边池时，界面正确显示每个池的金额和资格玩家。
- 摊牌后能清楚看到筹码从底池飞向赢家，赢家座位显示 +金额。
- 平局时两个赢家各自收到正确的份额，动画各自播放。
- 系统开启"减弱动态效果"时动画自动跳过，不影响结算结果。
```

---

### 提示词 11：补齐项目工程配置（P2-6）

```text
为 /Users/wuhaorui/WorkBuddy/本地虾/德 补齐缺失的工程配置文件。

【背景】
仓库根目录目前只有 index.html、server.js、public/、package.json、系统架构.md，缺少标准 Node.js 项目应有的配置文件，导致：
- 新人（或 AI）clone 后不知道怎么跑起来（无 README）；
- node_modules 和临时文件可能被误提交（无 .gitignore）；
- 部署到 Railway 时依赖手工在 Dashboard 上设置单实例，且没有健康检查端点（架构文档 §8.1 明确写了需要 replicas = 1，但仓库里没有对应配置文件）。

【要求】
1. `.gitignore`：忽略 node_modules/、.env、.DS_Store、*.log、npm-debug.log*。
2. `README.md`，包含：
   - 项目简介（多人在线德州扑克，Socket.io 实时同步，2-8 人，SB 10 / BB 20）；
   - 快速开始：`npm install` → `npm start` → 打开 http://localhost:3000 → 创建房间 → 复制分享链接；
   - 目录结构说明（明确 index.html 是单人练习版、public/index.html 是多人版前端）；
   - Socket 事件一览（指向系统架构.md，不重复维护）；
   - 部署说明（Railway：必须单实例，原因是有状态内存存储）；
   - 已知限制（内存存储、重启丢房间、无限 rebuy 等）。
3. `railway.toml`：`[deployments]` 下设置 `replicas = 1`，并配置 healthcheckPath。
4. `server.js` 增加 `GET /health` 端点，返回 `{ status: 'ok', rooms: Object.keys(rooms).length, uptime: process.uptime() }`，用于部署平台健康检查。
5. `package.json` 补充：`engines: { "node": ">=18" }`、`description`、`test` 脚本（若提示词 3 已完成则已有）。
6. `.env.example`：列出 PORT 及未来可能用到的配置（当前只有 PORT，写清楚默认值 3000）。

【验收标准】
- 全新 clone 到空目录后，照着 README 的两条命令就能跑起来并对局成功。
- `curl http://localhost:3000/health` 返回 200 和 JSON。
- `git status` 在 npm install 之后不出现 node_modules。
- railway.toml 提交后，Railway 部署时自动采用单实例配置。
```

---

### 提示词 12：房间清理与内存治理（P2-2）

```text
在 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js 中增加房间生命周期管理。

【背景】
`rooms` 是一个普通对象（第 24 行），只在"玩家全部离开"时才 `delete`。实际运行中存在泄漏：
- 房主创建房间后直接关页面，如果还有其他人留在房间里且一直不断线，房间永不释放；
- 房间创建后无人开始游戏、也无人加入，会一直占用内存；
- 进程重启后所有房间消失，但前端 localStorage 里还留着旧房间号，用户点进来只看到"房间不存在"，体验很差。

【要求】
1. 给 Room 增加 `lastActivityAt` 字段，在每次收到该房间任意 socket 事件时更新为 `Date.now()`。
2. 新增定时清理任务（每 5 分钟执行一次 `setInterval`）：
   - 删除 `players.length === 0` 的房间；
   - 删除 `lastActivityAt` 超过 2 小时的房间（无论是否在游戏中），删除前若有玩家在线，广播 `roomClosed` 事件告知前端；
   - 清理时同时 `clearTimeout` / `clearInterval` 掉该房间所有挂起的定时器（`_nextHandTimer`、`_disconnectTimer`），避免定时器泄漏和"向已删除房间广播"的错误。
3. 在清理日志里输出被删房间的原因和存活时长，便于观察。
4. 前端处理 `roomClosed`：弹出提示"房间已关闭（长时间无活动）"并返回大厅，同时清除 localStorage 里的旧房间号。
5. 前端处理 `joinRoom` 返回"房间不存在"时，不要只弹 alert，而是清掉 localStorage 里的旧房间号并自动跳回大厅（同样适用于"找不到房间"的场景）。

【验收标准】
- 创建 5 个房间后全部关闭页面，等待一个清理周期后 `GET /health` 里的 rooms 数量归零。
- 手动把某房间的 lastActivityAt 改成 3 小时前，清理周期触发后该房间被删除，在里面的人收到 roomClosed 提示并回到大厅。
- 长时间运行（比如用脚本连开 50 个房间再全部断线）后，Node 进程内存回落到基线附近，不持续增长。
```

---

### 提示词 13：下注流程的规则完整性（P2-4）

```text
完善 /Users/wuhaorui/WorkBuddy/本地虾/德/server.js 的下注规则，补上德州扑克标准规则里缺失的部分。

【背景】
在提示词 1（下注金额校验）和提示词 5（流程收拢）完成之后，下注流程还缺几条标准规则：
1. 短码 all-in 的加注额不足最小加注增量时，其他玩家只能跟注或弃牌，**不能重新加注**（当前无此限制）；
2. 一局结束时，未跟上的多余注额应当**退还**给多投的玩家（uncalled bet return），当前直接进了底池；
3. 边池的资格判定要考虑"弃牌玩家"，弃牌者的钱应进入对应层级但无资格赢取（现有逻辑已覆盖，需要补测试确认）；
4. 同步推进到摊牌时，若只剩一名玩家有资格赢主池，其余也应正确参与边池。

【要求】
1. 实现 `room.minRaiseAllowed`（布尔）：某次加注因短码 all-in 而未达到 `minRaise` 时置为 false，本轮内禁止再有人加注（只能 call / fold）；新一轮（advancePhase）或出现一次达标加注后恢复为 true。前端同步把"加注"按钮置灰。
2. 实现退还多余注额：在 `endHand`（或 advancePhase 进入下一阶段前）检查是否有人投入的 `currentBet` 高于其他人且无人跟注到该额度，若是则把差额退还给该玩家，从 pot 中扣除。
3. 为以上规则补充单元测试（test/betting.test.js，node:test），至少覆盖：
   - 短码 all-in 后被跟注，第三方不能重新加注；
   - 无人跟注的超额下注被退还；
   - 3 人不同筹码全下的边池拆分，各层级金额与资格正确；
   - 每局结束后 `sum(所有玩家 chips) + pot === 房间初始总筹码`（守恒断言）。

【验收标准】
- 筹码 100 / 500 / 1000 三人依次 all-in，主池与两个边池的金额和资格玩家完全正确。
- A（筹码 1000）下注 500，B、C 都弃牌，A 应被退还 500（或是按规则只赢走 blind 部分），底池金额与实际赢得的金额一致。
- 筹码守恒断言在连续 20 局随机模拟中恒成立（可写一个简单的随机 AI 对局脚本跑 20 局验证）。
```

---

### 提示词 14：单人练习版接入与归档决策（P2-5）

```text
处理 /Users/wuhaorui/WorkBuddy/本地虾/德/index.html（单人练习版）的定位问题。

【背景】
根目录的 index.html 是 2026-04-24 之前写的单人/人机练习版（1496 行，含发牌动画、切牌动画、快捷下注按钮 ½pot/pot、键盘快捷键等完整体验）。
问题在于：**Express 的 static 只挂载了 public/ 目录**（server.js 第 15 行），所以线上根本访问不到这个文件。它目前是一个"孤儿文件"，而且从 4 月 24 日之后就没有任何维护。

请按以下步骤处理，并在动手前先问我选 A 还是 B（如果无法询问，默认执行方案 A）：

【方案 A（推荐）：挂载为独立页面】
1. 把 index.html 移动到 `public/solo.html`（保持内容不变）。
2. 在 server.js 里确认 `/solo.html` 可以直接通过 static 访问；同时把根路径 `/` 保持指向多人版 `public/index.html`。
3. 在多人大厅界面底部加一行小字链接："单人练习模式 →"，点击跳转到 `/solo.html`；在单人版页面顶部加"返回多人对战 ←"链接。
4. 检查单人版的 UI 与多人版是否风格一致（配色、字体、按钮样式），不一致的话调整单人版去对齐多人版（不要动多人版）。
5. 更新 README 的目录结构说明。

【方案 B：归档】
1. 创建 `legacy/` 目录，把 index.html 移入并改名为 `legacy/solo-practice.html`。
2. 在 README 里说明这是历史版本、仅供参考、不再维护。

【额外要求（两个方案都要做）】
- 把单人版里已经做得比多人版好的东西"反向移植"到多人版：具体地，`½ pot / pot / 1½ pot / 2× pot` 快捷下注按钮的实现、发牌/切牌动画、键盘快捷键 F/C/B/R/A。相关代码请在单人版里定位后，移植到 public/index.html（注意单人版是本地状态、多人版是服务端状态，逻辑需要适配）。
- 移植后不要删除单人版里的原始实现。

【验收标准】
- 单人版可以通过明确 URL 访问到（方案 A：`/solo.html`）。
- 两个版本之间的跳转链接正常工作。
- 多人版出现快捷下注按钮且计算结果正确。
- 两个版本都能独立正常对局，互不影响。
```

---

### 提示词 15：产品化增强（阶段 5，按需取用）

```text
以下功能彼此独立，请**一次只做一项**，做完一项提交一次。项目根目录 /Users/wuhaorui/WorkBuddy/本地虾/德。

【15-A 计分板与 rebuy 统计】
在房间内增加计分板面板，显示每位玩家的：初始筹码、当前筹码、净盈亏（+/-）、rebuy 次数、手数、VPIP（可选）。
数据放在 room 对象里累计（`player.stats = { startChips, handsPlayed, rebuys, netWin }`）。
提供"重置统计"按钮（仅房主可见）。在 gameEnd 时同步更新。

【15-B 手牌历史（Hand History）】
服务端为每局记录一条精简记录：`{ handNo, 时间, 各玩家手牌（摊牌后记录）, 公共牌, 底池, 赢家, 牌型, 各家动作序列 }`，存在 `room.handHistory` 里（上限 50 条，超出丢弃最旧的）。
新增 socket 事件 `requestHistory` / 广播 `handHistory`。
前端提供"牌局回顾"抽屉，可翻看最近 20 局，含逐步动作回放（上一步/下一步按钮）。

【15-C 移动端体验重做】
当前移动端只是把牌面缩小 + 座位横向滚动，体验不佳。重做移动端布局：
- 竖屏：上方为对手座位（紧凑卡片），中部为公共牌+底池，下方固定为我的手牌和操作区（拇指可达），行动日志与聊天改为底部抽屉；
- 横屏：保持接近桌面端的布局；
- 操作按钮加大到至少 44x44px，下注滑块加粗触控区；
- 处理 iOS 安全区（env(safe-area-inset-bottom)）和键盘弹出时的布局跳动。

【15-D 音效与反馈】
用 Web Audio API（不要引入音频文件依赖）合成简单音效：发牌、下注筹码、弃牌、获胜、倒计时滴答、轮到你提示。
提供全局音效开关（存 localStorage）。默认开启但在首次交互后才允许播放（绕过浏览器自动播放限制）。
轮到自己时除了音效，还可以让浏览器标签页标题闪烁提示。

【15-E 房间设置】
允许房主在开始游戏前配置：初始筹码（1000/2000/5000，已支持）、盲注级别（10/20、25/50、50/100）、是否开启"盲注随时间自动上涨"、下注倒计时时长（15/30/60 秒/关闭）。
配置通过 `createRoom` 和新的 `updateRoomSettings` 事件下发，服务端在 startNewHand 时读取。

【验收标准】
- 每项功能都不破坏现有对局流程：改完后跑一局完整的多人对局确认无回归。
- 新增状态都要在 serializePlayer / 房间 payload 里正确下发，前端能正确渲染。
- 新增 socket 事件要同步更新 系统架构.md 的事件表。
```

---

## 五、执行建议

1. **先跑一遍再动手**：`npm install && npm start`，开两个浏览器窗口加同一房间打一局，亲自感受当前的手感和问题，后面改起来更有判断力。
2. **按提示词 1 → 2 → 3 → 4 顺序做阶段 1**，每完成一条 commit 一次，commit message 沿用现有风格（中文，说明修了什么）。
3. **提示词 5（重构）一定要在阶段 1 之后做**，否则会和下注校验的改动冲突。
4. **提示词 3 的测试文件必须先落地**：牌型是纯函数，测试成本极低但收益最高，能防止后续所有重构引入回归。
5. 每次提交前跑 `node --check server.js` 和 `npm test`（提示词 3 之后）。
6. 项目记忆写在 `.workbuddy/memory/` 下，重要修复请追加当日日志，方便下次接手时快速了解上下文。
